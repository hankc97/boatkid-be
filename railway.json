{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "package.json",
      "yarn.lock",
      "Dockerfile",
      "tsconfig.json",
      "src/**"
    ]
  },
  "deploy": {}
}
